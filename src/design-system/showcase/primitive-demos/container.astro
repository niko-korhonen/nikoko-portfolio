---
import DsContainer from '../../components/primitives/Container.astro';
---

<div style="background: var(--surface-3); border-radius: var(--radius-m); padding: var(--spacing-8);">
  <DsContainer>
    <p class="ds-type-code-m-tall" style="color: var(--foreground-secondary);">
      This text sits inside <code>Container</code> (max width + horizontal padding).
    </p>
  </DsContainer>
</div>
