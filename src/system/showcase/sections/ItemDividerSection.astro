---
import ItemDivider from '../../components/ItemDivider.astro';
import ItemStatic from '../../components/ItemStatic.astro';
import Stack from '../../primitives/Stack.astro';
import ComponentShowcasePage from '../templates/ComponentShowcasePage.astro';
import ShowcasePreviewCard from '../templates/ShowcasePreviewCard.astro';
import ShowcaseControlsTable from '../templates/ShowcaseControlsTable.astro';
import ShowcaseControlRow from '../templates/ShowcaseControlRow.astro';
import ShowcaseCopyPrompt from '../templates/ShowcaseCopyPrompt.astro';
---

<ComponentShowcasePage
  title="ItemDivider"
  headingId="item-divider-heading"
  description="Horizontal rule between list rows. Uses outline-tertiary token; place between headers, actions, or static rows without extra spacing primitives."
>
  <ShowcasePreviewCard>
    <div class="ds-item-demo" style="width:100%;max-width:28rem;">
      <Stack gap="space-2" align="stretch">
        <ItemStatic variant="floating">Above</ItemStatic>
        <ItemDivider />
        <ItemStatic variant="floating">Below</ItemStatic>
      </Stack>
    </div>
  </ShowcasePreviewCard>

  <ShowcaseControlsTable>
    <ShowcaseControlRow label="Usage">
      <p class="text-body-xs-plain" style="margin:0">
        Insert between rows; no props. Combine with ItemHeader and ItemAction for list chrome.
      </p>
    </ShowcaseControlRow>
  </ShowcaseControlsTable>

  <ShowcaseCopyPrompt componentLabel="ItemDivider" />
</ComponentShowcasePage>

<script>
  import { buildCopyPrompt } from '../copy-prompt';
  import { getShowcasePage } from '../showcase-ui';

  function init() {
    const anchor = document.querySelector('.ds-item-demo');
    const page = getShowcasePage(anchor);
    if (!page) return;

    page.querySelector('.js-ds-showcase-copy')?.addEventListener('click', async () => {
      const text = buildCopyPrompt({
        component: 'ItemDivider',
        props: {},
      });
      try {
        await navigator.clipboard.writeText(text);
      } catch {
        /* ignore */
      }
    });
  }

  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', init);
  else init();
</script>
