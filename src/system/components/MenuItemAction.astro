---
import ItemAction from './ItemAction.astro';

const { variant = 'inline', ...rest } = Astro.props;
---

<ItemAction {...rest} variant={variant}>
  <slot name="slot-start" slot="slot-start" />
  <slot />
  <slot name="slot-end" slot="slot-end" />
</ItemAction>
